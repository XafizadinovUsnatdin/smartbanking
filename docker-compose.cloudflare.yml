services:
  # Stable Cloudflare Tunnel (requires CLOUDFLARED_TOKEN)
  # Create a tunnel in Cloudflare Zero Trust -> Tunnels -> Create, then copy the token.
  cloudflared:
    command: ["tunnel", "--no-autoupdate", "run", "--token", "${CLOUDFLARED_TOKEN}", "--url", "http://api-gateway:8080"]
